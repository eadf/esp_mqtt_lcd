git stash

git checkout mqtt_branch
git pull
git checkout master
git merge --squash -s subtree --no-commit mqtt_branch
git commit -m "esp_mqtt upstream pull"

git checkout pcd8544_branch
git pull
git checkout master
git merge --squash -s subtree --no-commit pcd8544_branch
git commit -m "pcd8544 upstream pull"

git stash apply
